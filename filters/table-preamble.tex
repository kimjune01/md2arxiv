% Tables arrive as raw LaTeX (each HTML table is rendered to LaTeX in
% preprocessing), so the main pandoc run sees no native tables and does not
% auto-load the table packages. Include them explicitly.
\usepackage{longtable,booktabs,array}
\usepackage{calc}
